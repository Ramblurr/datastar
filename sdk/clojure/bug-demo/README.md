# bug-demo

This is a bug repro for INSERT-LINK.

- Spin it up with `clojure -M:run-m`. Optionally pass a port `clojure -M:run-m 8080` (the default is `3000`)

Or, for REPL explorers, jack in with your editor and load up the bug ns.
